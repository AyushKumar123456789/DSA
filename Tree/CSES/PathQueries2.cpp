// Heavy Light Decomposition

#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ll n, q;
    cin >> n >> q;
    vector<vector<ll>> graph(n + 1);
    vector<ll> values(n);
    for (ll i = 0; i < n; i++)
    {
        cin >> values[i];
    }
}